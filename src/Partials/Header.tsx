import React from 'react';
import { View, Text, Image } from 'react-native';

export default function Header() {
    return (
      <View>
            <Text>Задачи</Text>
            <Image src={}/>
      </View>
    );
}